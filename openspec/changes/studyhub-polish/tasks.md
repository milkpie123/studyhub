## 1. Database

- [x] 1.1 Add `in_coffee_break` boolean column (default false) to `profiles` table per "Coffee Break stored as seat_id = null + a separate `in_coffee_break` flag" design decision: create migration `supabase/migrations/004_coffee_break.sql` with `alter table public.profiles add column in_coffee_break boolean not null default false;` and update the existing `Users can update own profile` policy to include the new column
- [x] 1.2 Create `messages` table per "Messaging via Supabase table with Realtime subscription" design decision: create migration `supabase/migrations/005_messages.sql` with columns `id uuid primary key default gen_random_uuid()`, `sender_id uuid references auth.users not null`, `recipient_id uuid references auth.users not null`, `text text not null check (char_length(text) <= 100)`, `created_at timestamptz default now()`, `read_at timestamptz`; enable RLS; add policy "Sender can insert own messages" `for insert with check (auth.uid() = sender_id)`; add policy "Recipient can select own messages" `for select using (auth.uid() = recipient_id)`; add policy "Recipient can update own messages" `for update using (auth.uid() = recipient_id)`; enable replica identity full for Realtime

## 2. Click-to-sit replaces keyboard navigation

- [x] 2.1 Remove keyboard navigation from `components/room/StudyRoom.tsx` satisfying "No keyboard navigation hints": delete the `focusedSeat` state, the `ADJACENCY` constant, the `onKeyDown` handler, the `tabIndex`/`onKeyDown` props on the grid `<div>`, and the keyboard hint `<p>` element at the bottom of the room
- [x] 2.2 Update `components/room/Seat.tsx` to accept an `onClick` prop (type `() => void`) and attach it to the seat `<div>` root element; the seat must have `cursor-pointer` when empty and `cursor-default` on own seat
- [x] 2.3 Wire click-to-sit in `components/room/StudyRoom.tsx` per the "Click-to-sit replaces keyboard navigation" design decision: for each seat, pass `onClick` that (a) does nothing if seat is occupied by current user, (b) opens message popup if occupied by another user, (c) calls claim logic (`supabase.from('seats').upsert(...)`) and sets `in_coffee_break = false` on profiles if empty — satisfying "Click empty seat to claim", "User cannot claim an occupied seat", and "Claiming a seat clears Coffee Break status" requirements

## 3. Coffee Break area

- [x] 3.1 Create `components/room/CoffeeBreakArea.tsx`: renders a labeled panel with warm cozy styling (matching the study hall border/background palette — use `border border-[#e8dfc8] bg-[#fdf6e3] rounded-2xl p-4`); accepts `users: { display_name: string; avatar_color: string }[]` prop and renders each as an Avatar + display name; if `users` is empty shows placeholder text "quiet here… grab a cup ☕"; clicking anywhere on the panel (not on a user avatar) fires an `onBreakClick` prop callback
- [x] 3.2 Add `in_coffee_break` to profile data fetched in `app/(main)/page.tsx`: update the profiles select query to include `in_coffee_break`; pass it through to StudyRoom
- [x] 3.3 Subscribe to `profiles` Realtime changes in `components/room/StudyRoom.tsx` (alongside existing `seats` subscription): on any UPDATE event, update local state so other users' `in_coffee_break` flag is reflected without page reload — satisfying "Coffee Break avatars are visible to all users" requirement
- [x] 3.4 Wire Coffee Break click in `components/room/StudyRoom.tsx`: the `onBreakClick` callback calls `supabase.from('seats').delete().eq('seat_id', currentSeatId)` (if in a seat) then `supabase.from('profiles').update({ in_coffee_break: true }).eq('id', userId)`; satisfying "Clicking Coffee Break area moves user there" and "User moves to Coffee Break from a seat" requirements
- [x] 3.5 Render `CoffeeBreakArea` below the seat grid in `components/room/StudyRoom.tsx`, passing `users` as all profiles with `in_coffee_break = true` (filtered from local state) and `onBreakClick` — satisfying "Coffee Break zone exists below study hall" requirement

## 4. Preset messaging — popup

- [x] 4.1 Create `components/room/MessagePopup.tsx` satisfying "Message popup closes without sending": accepts `recipientName: string`, `onSend: (text: string) => void`, `onClose: () => void`; renders a small card with a heading "Send a message to {recipientName}", the 5 preset messages as buttons ("Hi there! 👋", "You got this! 💪", "How are you doing? 😊", "Keep up the great work! ⭐", "Taking a short break? 🍵"), and an × close button; clicking a message button calls `onSend(text)` and then `onClose`; pressing Escape or clicking the backdrop calls `onClose` — satisfying "User opens message popup" and "User sends a preset message" requirements
- [x] 4.2 Wire message popup in `components/room/StudyRoom.tsx` satisfying "User can send a preset message to another user": add `messageTarget: { id: string; display_name: string } | null` state; when a user clicks another user's avatar (occupied seat or Coffee Break avatar) set `messageTarget`; render `<MessagePopup>` when `messageTarget` is set; `onSend` handler calls `supabase.from('messages').insert({ sender_id: userId, recipient_id: messageTarget.id, text })` — satisfying "User cannot message themselves" requirement by only setting target when `seat.occupied_by !== userId`

## 5. Preset messaging — notification

- [x] 5.1 Create `components/room/MessageNotification.tsx` per "Heart dismiss animation" design decision satisfying "Heart button dismisses the notification with animation": accepts `id: string`, `senderName: string`, `text: string`, `onDismiss: (id: string) => void`; renders a card (white background, rounded, shadow, bottom-right positioning handled by parent); shows sender name, message text, and a heart button (🤍); clicking the heart: sets `heartRed` state to true (heart turns ❤️ red via CSS transition at 200 ms), then after 400 ms calls `onDismiss(id)` which triggers parent fade-out; also sets a 15-second `setTimeout` on mount that calls `onDismiss` if not already dismissed — satisfying "Auto-dismiss after 15 seconds" requirement
- [x] 5.2 Add message Realtime subscription in `components/room/StudyRoom.tsx`: subscribe to `messages` table filtered `recipient_id=eq.{userId}` on INSERT; on event, add incoming message (id, sender_id, text) to `notifications` state array; look up sender display name from local profile state; cap array at 3, dropping the oldest if exceeded — satisfying "Received messages appear as bottom-right notification cards" and "Maximum 3 cards visible" requirements
- [x] 5.3 Render notification stack in `components/room/StudyRoom.tsx`: absolutely position a `<div>` in the bottom-right corner (`fixed bottom-4 right-4 flex flex-col-reverse gap-2 z-50`); map `notifications` to `<MessageNotification>` components with fade-in animation (`transition-opacity duration-300`); `onDismiss` removes the entry from state

## 6. Profile modal

- [x] 6.1 Create `components/profile/ProfileModal.tsx` per "Profile modal reads from profiles and tasks tables" design decision satisfying "Profile modal shows animal picker and editable display name" and "Profile modal can be closed without saving": accepts `profile: { id: string; display_name: string; avatar_color: string; created_at: string }`, `tasksDoneCount: number`, `onClose: () => void`, `onSave: (updates: { display_name: string; avatar_color: string }) => void`; renders a modal overlay with × button and backdrop click to close (discard changes); shows animal picker grid (same 8 animals from ANIMALS constant, same 4-column grid style as signup page); shows display name `<input>` pre-filled with current name; shows "Days on site: N" where N = `Math.max(1, Math.floor((Date.now() - new Date(profile.created_at).getTime()) / 86400000))`; shows "Tasks completed: N"; Save button calls `onSave` only if display_name or avatar_color changed, then closes — satisfying "Profile modal shows days on site and total finished tasks" and all other profile-modal requirements
- [x] 6.2 Wire profile modal in `components/room/StudyRoom.tsx` (or in `app/(main)/page.tsx` layout): add `profileOpen` boolean state; render user's own display name as a `<button>` that sets `profileOpen = true`; render `<ProfileModal>` when open; `onSave` calls `supabase.from('profiles').update({ display_name, avatar_color }).eq('id', userId)` then updates local profile state — satisfying "User can open their profile modal by clicking their display name" requirement
- [x] 6.3 Fetch `tasksDoneCount` for profile modal: in the component or effect that opens the modal, call `supabase.from('tasks').select('id', { count: 'exact', head: true }).eq('user_id', userId).eq('done', true)` and pass the count to `ProfileModal` — satisfying "Profile modal shows days on site and total finished tasks" requirement
